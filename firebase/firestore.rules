rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Spherepath is API-first. Firebase Auth supplies identity tokens, while
    // callable Functions authorize and execute every domain query/command.
    // Admin SDK requests from Functions bypass these client rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
